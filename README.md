# AImod Wendy 角色 mod 生成器

该项目已改造为完整角色 mod 生成工具，使用你上传的 Wendy mod 模板目录 `wendyMod/dst-wendy-pub`。

## 功能

- 直接使用完整 `Wendy` 角色 mod 模板
- 修改 `modinfo.lua` 中的名称、描述、作者和版本
- 打包为完整可下载 `ZIP` 文件

## 安装与运行

```bash
npm install
npm run dev
```

打开浏览器访问 `http://localhost:4173`，填写 mod 信息并生成完整 Wendy 角色 mod ZIP。
