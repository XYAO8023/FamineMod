# AImod famine 角色 mod 生成器

该项为角色 mod 生成工具

## 功能

- 通过秒速实现 任务mod生成。
- 交互填写 `modinfo.lua` 中的名称、描述、作者和版本
- 打包为完整可下载 `ZIP` 文件

## 安装与运行

```bash
npm install
npm run dev
```

打开浏览器访问 `http://localhost:4173`，填写 mod 信息并生成完整 mod 角色 ZIP。
